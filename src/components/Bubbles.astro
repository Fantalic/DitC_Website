---
const count = 45;

---
<!-- <div id="animatedElement" class="text-white w-10 h-10 bottom-10 z-60 absolute"> HALLO</div> -->

<div class="bubbles">

  {Array.from({ length: count }, (_, i) => {
    const size = 50*Math.random();
    const animationDuration = 30*(Math.round(Math.random())+2 )
    const offset ={
      top:50*Math.random()-25+"vh",
      position: "relative",
      width: size +"px",
      height: size+"px",
    }
    const style = {
      opacity:  0.3*Math.random()+0.1,
      left: 120*Math.random()-10+"%",
      animationDelay: 60*Math.random()+"s",
      animationDuration: animationDuration+"s",
    }

    return (
        <span style={style} >
          <div style={offset}></div>
        </span>
    )
    })}
</div>
<script>
    // const element = document.getElementById('animatedElement');
    // const duration = 3000; // Duration of the animation in milliseconds
    // const startTime = performance.now(); // Get the current time
    // console.log(element)

    // function animate(time) {
    //     const elapsed = time - startTime;

    //     // Calculate progress (0 to 1)
    //     const progress = Math.min(elapsed / duration, 1);

    //     // Define your function f(x)
    //     const a = -200; // Coefficient for the parabola
    //     const b = 400; // Linear coefficient
    //     const c = 100; // Constant term

    //     // Calculate x position based on progress
    //     const x = progress * window.innerWidth; // Move across the screen
    //     const y = a * Math.pow(x / window.innerWidth, 2) + b * (x / window.innerWidth) + c; // f(x)

    //     // Set the new position of the element
    //     element.style.transform = `translate(${x}px, ${y}px)`;

    //     // Continue animating until duration is reached
    //     if (progress < 1) {
    //         requestAnimationFrame(animate);
    //     }
    // }

    // Start the animation
    // requestAnimationFrame(animate);
</script>

<style>

.bubbles {
  position: relative;
  width: 100%;
  top:25vh;
  animation: bubbles  0s linear infinite;
}

@keyframes bubbles {
  0%, 100% {transform: rotate(-0.5deg);}
  50% {transform: rotate(0.5deg);}
}

.bubbles div {
  border-radius: 100%;
  background-color: white;
}
.bubbles span {
  position: absolute;
  animation: bubble 1s ease-in-out infinite;
}

@keyframes bubble {
  0% {bottom: 0; opacity: 0;}
  100% {bottom: 550vh; }
}

</style>