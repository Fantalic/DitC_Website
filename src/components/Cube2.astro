
<svg viewBox="-100 -100 200 200" class="cube">
    <!-- Front face -->
    <polygon points="-50,-50 50,-50 50,50 -50,50" fill="rgba(255, 0, 0, 0.9)" />
    <!-- Back face -->
    <polygon points="-50,-50 -100,-100 -100,0 -50,0" fill="rgba(0, 255, 0, 0.9)" transform="translateZ(-100)" />
    <!-- Left face -->
    <polygon points="-50,-50 -100,-100 -100,-50 -50,0" fill="rgba(0, 0, 255, 0.9)" />
    <!-- Right face -->
    <polygon points="50,-50 100,-100 100,-50 50,0" fill="rgba(255, 255, 0, 0.9)" />
    <!-- Top face -->
    <polygon points="-50,-50 -100,-100 -50,-100" fill="rgba(255, 165, 0, 0.9)" />
    <!-- Bottom face -->
    <polygon points="-50,50 -100,0 -50,0" fill="rgba(128, 128, 128, 0.9)" />
</svg>
<style>
    body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #f4f4f4;
        margin: 0;
    }

    .cube {
        animation: rotate 5s infinite linear;
        transform-style: preserve-3d;
    }

    @keyframes rotate {
        from {
            transform: rotateX(0deg) rotateY(0deg);
        }
        to {
            transform: rotateX(360deg) rotateY(360deg);
        }
    }

    svg {
        width: 200px;
        height: 200px;
    }
</style>