<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Persistent Hover</title>
  <style>
    /* Hide the checkbox */
    .toggle {
      display: none;
    }

    /* Default box styles */
    .box {
      width: 100px;
      height: 100px;
      background-color: lightblue;
      border: 2px solid darkblue;
      transition: background-color 0.3s ease, border-color 0.3s ease;
      cursor: pointer;
    }

    /* Apply the hover effect */
    .box:hover,
    .toggle:checked + .box {
      background-color: orange;
      border-color: red;
    }
  </style>
</head>
<body>
  <!-- Hidden checkbox -->
  <input type="checkbox" id="toggle" class="toggle">
  <!-- Label styled as a box -->
  <label for="toggle" class="box"></label>
</body>
</html>
